name: pizza_shack
description: "Pizza Shack mobile app."

publish_to: 'none'

version: 0.0.1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.30.1
  cloud_firestore: ^4.17.2

  provider: ^6.1.2
  cached_network_image: ^3.3.1
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo_text.png 